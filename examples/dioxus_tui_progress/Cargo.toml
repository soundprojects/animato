[package]
name = "dioxus_tui_progress"
version = "0.1.0"
edition = "2024"

[dependencies]
animato = { path = "../../crates/animato", features = ["dioxus-desktop"] }
dioxus = { version = "0.7.9", default-features = false, features = ["desktop", "launch"] }
